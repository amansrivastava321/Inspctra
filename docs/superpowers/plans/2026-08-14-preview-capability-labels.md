# Preview Capability Labels Implementation Plan

1. Add failing tests for policy classification, badge rendering, disabled preview choices, legacy read-only/run gating, preview schedules, and preview app targets.
2. Add the shared capability policy and `PreviewBadge` component.
3. Integrate the capability selector and legacy enforcement into validation pack detail.
4. Integrate schedule labeling, app-type labeling, and run-target protection.
5. Run focused tests, the full frontend suite, and the production build.
6. Verify the create-pack and legacy-pack states in the browser, then rebuild Graphify.

