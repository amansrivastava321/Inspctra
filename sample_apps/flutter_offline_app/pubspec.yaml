name: flutter_offline_app
description: Intentionally flawed flutter placeholder for QA-AI benchmarking.
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.0
